{
  "name": "message-client",
  "version": "1.0.0-4",
  "description": "A message client that connects to a message broker and sends and receives messages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "git push --follow-tags"
  },
  "keywords": [
    "micro-service",
    "messaging"
  ],
  "author": "Taariq Mullins",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.5.2",
    "async": "^2.6.0",
    "msgpack": "^1.0.2",
    "rabbitmq-node": "^0.5.0",
    "uniqid": "^4.1.1"
  },
  "devDependencies": {
    "semantic-release-gitlab": "^4.1.3"
  }
}
